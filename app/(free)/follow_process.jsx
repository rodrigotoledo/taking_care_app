import React from 'react';
import { Text, View } from 'react-native';

const FollowProcess = ({
    params,
}) => (
    <View>
        <Text>FollowProcess</Text>
    </View>
);

export default FollowProcess;
